#!/bin/sh

start_once() {
  command -v "$1" >/dev/null 2>&1 || return 0
  pgrep -u "$USER" -x "$1" >/dev/null 2>&1 || "$@" &
}

# Status bar and notifications.
start_once dunst
start_once waybar

exec fluxbox
